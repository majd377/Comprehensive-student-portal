# دليل استكشاف الأخطاء - البيانات لا تظهر من Firebase

## 🔍 خطوات التشخيص:

### 1. افتح Developer Console (مهم جداً!)
- اضغط **F12** أو **Ctrl+Shift+I**
- اذهب لتبويب **Console** (الكونسول)
- ستجد رسائل تخبرك عن حالة Firebase

### 2. رسائل التشخيص المتوقعة:

**✅ إذا كانت الأمور تعمل:**
```
✅ Firebase initialized successfully
📥 Data loaded from Firebase: {...}
☁️ Data saved to Firebase
```

**⚠️ إذا كانت هناك مشكلة في الاتصال:**
```
❌ Firebase initialization error: ...
❌ Error loading data from Firebase: ...
```

---

## 🛠️ الحلول الممكنة:

### المشكلة #1: البيانات محفوظة محلياً فقط (localStorage)
**السبب:** لا يوجد اتصال بـ Firebase أو مشكلة في الأذونات

**الحل:**
1. افتح الكونسول (F12)
2. تحقق من الرسائل الحمراء
3. جرب الأوامر التالية في الكونسول:

```javascript
// تحقق من البيانات المحلية
localStorage.getItem('collegeAppData')

// جرب الاتصال بـ Firebase
firebase.database().ref('appData').once('value').then(snap => {
  console.log('Firebase response:', snap.exists() ? snap.val() : 'No data');
}).catch(err => console.error('Firebase error:', err));
```

### المشكلة #2: إعادة تحميل الصفحة تفقد البيانات
**السبب:** البيانات تُحفظ محلياً فقط، وليس في Firebase

**الحل:**
1. تأكد من الاتصال بالإنترنت
2. افتح الكونسول وتحقق من رسائل الخطأ
3. جرب القيام بتحديث صغير (مثل تغيير اسم مادة)
4. تحقق من ظهور الرسالة ☁️ "Data saved to Firebase"

### المشكلة #3: البيانات موجودة لكن لا تُعرض
**السبب:** قد تكون هناك مشكلة في جزء عرض البيانات (render)

**الحل:**
1. تأكد من أن الشعبة المختارة صحيحة
2. افتح الكونسول واكتب:
```javascript
console.log('Current section:', selectedSection);
console.log('App data:', appData);
console.log('Section data:', appData.progress[selectedSection]);
```

---

## 📋 معلومات Firebase الحالية:

```
Project: mycollegeapp-e5362
Database URL: https://mycollegeapp-e5362-default-rtdb.firebaseio.com
```

### الوصول إلى بيانات Firebase عبر الويب:
إذا كانت الأذونات تسمح بالقراءة العامة:
https://mycollegeapp-e5362-default-rtdb.firebaseio.com/appData.json

---

## ✅ قائمة التحقق:

- [ ] الكونسول لا يظهر أخطاء حمراء
- [ ] ترى الرسالة "✅ Firebase initialized successfully"
- [ ] عند الحفظ، ترى "☁️ Data saved to Firebase"
- [ ] البيانات موجودة في الصفحة بعد إضافتها
- [ ] إعادة تحميل الصفحة (Ctrl+R) تحتفظ بالبيانات

---

## 🔐 ملاحظة أمان:

الـ API Key ظاهرة في الكود (وهي آمنة نسبياً لـ Firebase Realtime Database).
تأكد من ضبط قواعد الأمان في Firebase Console:
https://console.firebase.google.com/project/mycollegeapp-e5362/database/rules
