# 🔍 أداة التشخيص الشاملة للبيانات

## المشكلة:
البيانات المحفوظة في Firebase لا تظهر عند فتح الموقع.

---

## ✅ الحل - خطوات سهلة:

### الخطوة 1️⃣: افتح أدوات المطور
اضغط **F12** أو **Ctrl+Shift+I** (في Windows)

### الخطوة 2️⃣: انقر على تبويب **Console** (الكونسول)

### الخطوة 3️⃣: اكتب الأمر التالي ثم اضغط Enter:
```
diagnose()
```

---

## 📊 ماذا سترى:

ستظهر لك معلومات مفصلة عن:

1. **📡 حالة Firebase**
   - هل يوجد اتصال بـ Firebase؟
   - هل قاعدة البيانات متصلة؟

2. **💾 Local Storage**
   - البيانات المحفوظة محلياً
   - عدد الشعب والمواد والواجبات

3. **📦 بيانات التطبيق الحالية**
   - الشعبة المختارة
   - الصفحة الحالية
   - التقدم والواجبات

4. **🌐 حالة الاتصال**
   - هل أنت متصل بالإنترنت؟

5. **🖥️ معلومات المتصفح**

---

## 🔧 الخطوات اليدوية في الكونسول:

### 1️⃣ تحقق من البيانات المحلية:
```javascript
// عرض جميع البيانات المحلية
console.log(localStorage.getItem('collegeAppData'))
```

### 2️⃣ تحقق من البيانات الحالية:
```javascript
// عرض بيانات التطبيق
console.log(appData)

// عرض تقدم الشعبة الحالية
console.log(appData.progress[selectedSection])

// عرض واجبات الشعبة الحالية
console.log(appData.assignments[selectedSection])
```

### 3️⃣ تحقق من الاتصال بـ Firebase:
```javascript
// اختبر الاتصال
firebase.database().ref('appData').once('value').then(snap => {
  console.log('البيانات من Firebase:', snap.val());
}).catch(err => {
  console.error('خطأ:', err);
});
```

### 4️⃣ أعد حفظ البيانات يدويًا:
```javascript
// احفظ البيانات الحالية
saveData()
// يجب أن ترى: "☁️ Data saved to Firebase"
```

### 5️⃣ أعد تحميل البيانات من Firebase:
```javascript
// أعد تحميل من Firebase
db.ref('appData').once('value').then(snap => {
  appData = normalizeAppData(snap.val() || defaultData);
  render();
  console.log('تم التحديث من Firebase');
});
```

---

## 🚨 إذا كانت هناك أخطاء:

### الخطأ: "Firebase is not defined"
✅ **الحل:** أعد تحميل الصفحة (Ctrl+R)

### الخطأ: "Permission denied"
✅ **الحل:** قد تكون مشكلة في قواعد الأمان في Firebase
- تحقق من: https://console.firebase.google.com/project/mycollegeapp-e5362/database/rules

### الخطأ: "Network error"
✅ **الحل:** 
- تحقق من الاتصال بالإنترنت
- جرب في متصفح مختلف
- امسح ذاكرة التخزين المؤقتة (Ctrl+Shift+Delete)

---

## 💡 نصائح مهمة:

✅ **البيانات تُحفظ محلياً أولاً** ثم في Firebase
- لذلك حتى لو حدثت مشكلة في الاتصال، البيانات آمنة محلياً

✅ **إذا أعدت تحميل الصفحة** ستُحمّل البيانات تلقائياً من:
1. Firebase أولاً (إذا كان الاتصال موجوداً)
2. localStorage ثانياً (كنسخة احتياطية)

✅ **لا تقلق من البيانات** - لديك:
- نسخة محلية في `localStorage`
- نسخة في Firebase
- كود يحافظ على النسخ الاحتياطية

---

## 📞 معلومات Firebase:

```
Project ID: mycollegeapp-e5362
Database URL: https://mycollegeapp-e5362-default-rtdb.firebaseio.com
```

### عرض البيانات مباشرة:
إذا كانت الأذونات تسمح:
https://mycollegeapp-e5362-default-rtdb.firebaseio.com/appData.json

---

## 🎯 الخطوات إذا استمرت المشكلة:

1. ✅ افتح الكونسول واكتب `diagnose()`
2. ✅ أرسل لي الرسائل الحمراء (الأخطاء)
3. ✅ أخبرني بـ:
   - هل تراها البيانات في localStorage؟
   - هل الاتصال بالإنترنت موجود؟
   - هل Firebase يستجيب؟

---

## 📝 ملاحظة نهائية:

النظام الآن:
- ✅ يحفظ البيانات محلياً أولاً
- ✅ يرسلها لـ Firebase ثانياً
- ✅ إذا حدث خطأ في Firebase، لا تفقد البيانات
- ✅ عند إعادة تحميل الصفحة، يحمّل من Firebase أو localStorage

**إذا كنت تفقد البيانات عند إعادة التحميل = مشكلة في Firebase أو الأذونات**
